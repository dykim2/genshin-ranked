import { createTheme } from "@mui/material/styles";

const ButtonTheme = createTheme({
    typography: {
        button: {
            textTransform: 'none'
        }
    },
})

export default ButtonTheme;